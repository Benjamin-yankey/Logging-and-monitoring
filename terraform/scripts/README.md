# Setup Scripts

This directory contains shell scripts used during the provisioning process to initialize and configure servers.

## Contents

- `app-server-setup.sh`: Configures the application server environment.
- `monitoring-setup.sh`: Sets up the monitoring stack on the monitoring instance.
